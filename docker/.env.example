# Biến cho docker-compose (KHÔNG phải env của backend)
# Copy: cp .env.example .env

POSTGRES_USER=toolautofb
POSTGRES_PASSWORD=toolautofb
POSTGRES_DB=tool_auto_fb

# Port map ra host. Dùng port lệch chuẩn để không đụng Postgres/Redis sẵn có trên máy.
POSTGRES_PORT=55432
REDIS_PORT=56379
