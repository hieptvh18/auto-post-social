# URL gốc của backend API (đã gồm prefix /api). Dev proxy Vite trỏ /api → đây.
VITE_API_BASE_URL=/api

# true = dùng dữ liệu mock (MockDataContext), không cần backend.
# false = gọi backend thật qua VITE_API_BASE_URL.
VITE_USE_MOCK=false
